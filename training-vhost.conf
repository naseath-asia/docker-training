<VirtualHost *:80>

	ServerName training.cs.docker
	DocumentRoot /var/www

	<Directory />
		Options FollowSymLinks
		DirectoryIndex index.php index.html
		AllowOverride all
		Order allow,deny
		Allow from all
		Require all granted
	</Directory>

</VirtualHost>
